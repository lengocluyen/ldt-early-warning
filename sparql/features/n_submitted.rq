PREFIX ldt: <http://example.org/ldt#>

SELECT ?learner (COUNT(DISTINCT ?assessment) AS ?n_submitted)
WHERE {
  ?learner a ldt:Learner .
  OPTIONAL {
    ?learner ldt:submits ?result .
    ?result a ldt:AssessmentResult ;
            ldt:concernsAssessment ?assessment .
  }
}
GROUP BY ?learner
